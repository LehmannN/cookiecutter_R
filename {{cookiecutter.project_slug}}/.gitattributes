# Write here the type of file that you want to be handled by Git LFS 
# Note : if you don't wnat those files to be handled by Git LFS, you can add the file extensions to your .gitignore file 

*.csv 	filter=lfs diff=lfs merge=lfs -text
*.tsv 	filter=lfs diff=lfs merge=lfs -text
#*.h5 	filter=lfs diff=lfs merge=lfs -text
#*.rds 	filter=lfs diff=lfs merge=lfs -text
#*.RData 	filter=lfs diff=lfs merge=lfs -text